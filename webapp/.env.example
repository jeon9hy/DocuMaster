# 복사해서 .env.local로 쓰세요. 실제 값(.env.local)은 Git에 올리지 않습니다.
# 지금 프로토타입은 목업 서비스만 쓰므로 아래 값은 아직 읽지 않습니다.

# 실제 오케스트레이터 API 주소 (services/index.ts에서 HttpWorkspaceService를 만들 때 사용 예정)
NEXT_PUBLIC_API_URL=

# LLM API 키는 브라우저 코드(NEXT_PUBLIC_)에 두지 않고 서버(백엔드)에만 둡니다.
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
