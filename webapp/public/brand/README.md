# brand — 로고

| 파일 | 쓰는 곳 |
| --- | --- |
| `logo-mark.svg` | 헤더 로고 심볼, 브라우저 탭 아이콘 |

원본 PNG로 바꾸려면 이 폴더에 `logo-mark.png`, `logo-wordmark.png`를 넣고
`src/constants/brand.ts`의 경로를 바꾸세요. 워드마크(`wordmarkSrc`)를 비워 두면 글자로 그립니다.
