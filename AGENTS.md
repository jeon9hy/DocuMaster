# Codex 개발 지침

이 저장소에서 Codex는 지침과 코드의 개발·유지보수를 맡는다. 문서·발표자료 생성 시의 모델 구성과 역할, 검증 게이트는 `CLAUDE.md`와 `.claude/`가 정한다. 개발 작업을 시작했다고 그 실행 흐름을 Codex 중심으로 바꾸지 않는다.

## 작업 범위

- 웹앱은 `webapp/`에서 개발한다. 구조와 실행·검사 명령은 `webapp/README.md`를 확인한다. `webapp/AGENTS.md`의 Next.js 버전별 지침도 따른다.
- 생성 파이프라인 지침을 바꿀 때는 `CLAUDE.md` §9의 파일별 책임을 따른다. 팀·게이트·배선은 `CLAUDE.md`, 공통 기준과 역할·절차는 `.claude/`, 사용법은 `README.md`에 둔다. 관련된 기존 문구도 함께 갱신해 모순을 남기지 않는다.
- `작업/<ID>/workspace/05_*`는 검증된 사실, `06_*`는 구조의 기준이다. 기존 작업을 수정할 때 이 계약과 `상태.md`·`기록.md`의 판정을 존중한다.
- `자료/`, `작업/`, `최종/`의 사용자 자료·결과물과 `webapp/.data/`의 로컬 DB·로그·계정 상태를 개발용 초기화나 재생성 대상으로 취급하지 않는다. 필요한 테스트는 별도 임시 경로 또는 가짜 오케스트레이터를 사용한다.

## 변경 확인

- 변경 전후 `git status`를 확인하고 기존 사용자의 변경을 보존한다.
- 웹앱 변경은 영향 범위에 맞춰 `webapp/README.md`의 테스트·타입 검사·lint·빌드 명령으로 확인한다. 실제 문서 생성 모델을 호출하는 검증은 해당 통합을 변경했을 때만 필요하다.
- `DOCUMASTER_ORCHESTRATOR=claude`는 기존 실제 실행 설정이며, 현재 웹앱은 `fake`와 `claude`만 지원한다. 실행 엔진 교체가 명시적으로 요청되지 않은 개발 작업에서는 이 계약을 유지한다.
