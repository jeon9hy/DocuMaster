# 재생성 가능·대용량
__pycache__/
node_modules/
package-lock.json
tmp/

# 사용자 참고자료(저작권·개인 자료)
자료/

# 옛 원문(개인 계정·경로 포함, 규칙상 읽지 않음)
.claude/archive/

# 작업 폴더: 산출 문서(workspace/*.md)만 올린다. 로그·세션 ID·중간 산출물은 로컬에만
작업/*/*
!작업/*/workspace/
작업/*/workspace/*
!작업/*/workspace/*.md
